services:
  mealplanner:
    build: .
    restart: unless-stopped
    expose:
      - "8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    networks:
      - default
      - jellyfin_default

networks:
  jellyfin_default:
    external: true
